<%= render 'layerbegin' %>

<div class="page-content">
  <div class="page-header">
    <h1>
      <%= @withdrawapp.user.name %>(<%= @withdrawapp.user.login %>)的提现处理
    </h1>
  </div><!-- /.page-header -->
  <%= render 'form', withdrawapp: @withdrawapp %>

</div>

<%= render 'layerend' %>
